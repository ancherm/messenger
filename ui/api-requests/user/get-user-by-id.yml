info:
  name: get-user-by-id
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8888/api/users/1
  headers:
    - name: Authorization
      value: Bearer {{tokenLogin}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
