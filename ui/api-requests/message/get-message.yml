info:
  name: get-message
  type: http
  seq: 5

http:
  method: get
  url: http://localhost:8888/api/messages/{{msgId}}
  headers:
    - name: Authorization
      value: Bearer {{tokenLogin}}
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
