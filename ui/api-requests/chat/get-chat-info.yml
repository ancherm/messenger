info:
  name: get-chat-info
  type: http
  seq: 6

http:
  method: get
  url: http://localhost:8888/api/chats/{{chatId}}
  headers:
    - name: Authorization
      value: Bearer {{tokenLogin}}
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
