info:
  name: delete-or-leave-from-chat
  type: http
  seq: 8

http:
  method: delete
  url: http://localhost:8888/api/chats/{{chatId}}
  headers:
    - name: Authorization
      value: Bearer {{tokenLogin}}
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
