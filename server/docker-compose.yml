version: '3.8'

services:
  postgres-db:
    image: postgres
    container_name: messenger-db
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql
    restart: unless-stopped

volumes:
  pgdata:
