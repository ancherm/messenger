info:
  name: delete-message
  type: http
  seq: 7

http:
  method: delete
  url: http://localhost:8888/api/messages/{{msgId}}
  headers:
    - name: Authorization
      value: Bearer {{tokenLogin}}
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
