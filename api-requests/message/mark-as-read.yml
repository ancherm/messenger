info:
  name: mark-as-read
  type: http
  seq: 12

http:
  method: post
  url: http://localhost:8888/api/chats/{{chatId}}/messages/{{msgId}}/read
  headers:
    - name: Authorization
      value: Bearer {{tokenLogin}}
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
