info:
  name: get-who-read-message
  type: http
  seq: 13

http:
  method: get
  url: http://localhost:8888/api/chats/{{chatId}}/messages/{{msgId}}/readers
  headers:
    - name: Authorization
      value: Bearer {{tokenLogin}}
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
