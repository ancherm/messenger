info:
  name: change-role
  type: http
  seq: 2

http:
  method: patch
  url: http://localhost:8888/api/chats/1/participants/2
  headers:
    - name: Authorization
      value: Bearer {{tokenLogin}}
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "role": "ADMIN"
      }
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
