info:
  name: pin-chat
  type: http
  seq: 9

http:
  method: post
  url: http://localhost:8888/api/chats/{{chatId}}/pin
  headers:
    - name: Authorization
      value: Bearer {{tokenLogin}}
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
