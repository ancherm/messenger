info:
  name: unpin-chat
  type: http
  seq: 10

http:
  method: delete
  url: http://localhost:8888/api/chats/{{chatId}}/pin
  headers:
    - name: Authorization
      value: Bearer {{tokenLogin}}
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
