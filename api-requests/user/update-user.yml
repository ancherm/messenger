info:
  name: update-user
  type: http
  seq: 4

http:
  method: PATCH
  url: http://localhost:8888/users/me
  headers:
    - name: Authorization
      value: Bearer {{tokenLogin}}
  body:
    type: json
    data: |-
      {
        "firstName": "Иван",
        "lastName": "Иванов",
        "phone": "+79990001122",
        "status": "ONLINE"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
